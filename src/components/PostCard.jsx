import React from 'react';
import PropTypes from 'prop-types';

const PostCard = () => {
    return (
        <div>
            PostCard
        </div>
    );
};

PostCard.propTypes = {};

export default PostCard;